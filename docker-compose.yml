version: '3.6'

services:
    app:
        build:
            context: ./
            dockerfile: app/Dockerfile
        network_mode: "host"
        volumes:
            - ./app/:/app/
        tty: true
        container_name: selfplay
        cap_add:
            - SYS_PTRACE
